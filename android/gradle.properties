org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Fix Kotlin incremental compile when project (D:) and pub cache (C:) are on different drives
kotlin.incremental=false
kotlin.compiler.preciseCompilationResultsBackup=false
# Flutter enables R8 by default but its default ProGuard file path fails on AGP 9.
# Release minification is configured explicitly in app/build.gradle.kts instead.
shrink=false
