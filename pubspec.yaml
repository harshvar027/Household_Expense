name: household_expense
description: "Household expense tracker — budgets, imports, analytics, and secure on-device storage."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite_sqlcipher: ^3.4.0
  flutter_secure_storage: ^10.3.1
  sqlite3: ^2.7.6
  sqlite3_flutter_libs: ^0.5.32
  path: ^1.9.1
 
  fl_chart: ^0.68.0
  shared_preferences: ^2.5.3


  file_selector: ^1.0.3
  
  csv: ^6.0.0
  pdf: ^3.11.1
  printing: ^5.13.4
  share_plus: ^13.2.0
  path_provider: ^2.1.6
  url_launcher: ^6.3.2
  flutter_email_sender: 10.0.1
  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2
  excel: ^4.0.6
  syncfusion_flutter_pdf: ^33.2.13
  excel2003: ^1.0.0
  google_mobile_ads: ^9.0.0
  in_app_purchase: ^3.2.3
  local_auth: ^2.3.0
  spreadsheet_decoder: ^2.3.0
  cryptography: ^2.7.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true